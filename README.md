# obsidian_smart_close
Fixes a long-standing Obsidian annoyance: pressing `Cmd+W` / `Ctrl+W` while a  sidebar panel is focused accidentally closes it. This plugin adds a command that  only closes tabs in the main editor — sidebar panels are ignored.
